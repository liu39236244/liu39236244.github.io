# go resource 方式

CREATE EXTERNAL RESOURCE "minio_resource"
PROPERTIES
(
    "type" = "s3",
    "s3.endpoint" = "http://192.168.5.100:9000",
    "s3.access_key" = "minioadmin",
    "s3.secret_key" = "minioadmin",
    "s3.use_path_style" = "true" -- MinIO 通常需要
);
